# Storage backend selection: local (default) or r2
STORAGE_BACKEND=local

# Local storage path (relative to backend working directory)
LOCAL_STORAGE_PATH=./storage

# Cloudflare R2 (S3 compatible, public bucket運用を想定)
# 必須: バケット公開設定 + CORS 設定（フロントのオリジンを許可）
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# 任意: 公開URL (例: https://<bucket>.<account_id>.r2.dev)
# 未設定でも <bucket>.<account_id>.r2.cloudflarestorage.com で組み立てます
R2_PUBLIC_BASE_URL=

# Database (SQLite default / PostgreSQLなどに差し替え可)
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/dbname
