# Storage backend selection: local or r2
STORAGE_BACKEND=local

# Local storage path (relative to backend working directory)
LOCAL_STORAGE_PATH=./storage

# Cloudflare R2 (S3 compatible)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# Optional: public base URL for direct access (if bucket is public)
R2_PUBLIC_BASE_URL=
