# When running inside Docker the frontend is served behind Nginx which proxies API calls
# through /api -> backend. Override this value for custom deployments.
# 開発時ローカルAPIを直接叩く場合は http://127.0.0.1:8000 などに上書きする
VITE_API_BASE_URL=/api
