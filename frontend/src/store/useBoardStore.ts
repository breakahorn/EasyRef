import { create } from 'zustand';

// This is a placeholder for board state management.
// We will implement this when we build the board feature.
export const useBoardStore = create(() => ({
  activeBoardId: null,
  addItemToBoard: () => console.log('addItemToBoard not implemented'),
}));
